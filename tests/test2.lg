import test1.lg

greaterthanone(2).
greaterthanone(X) plif greaterthanone(X-1).

int fact(int n){
    if(greaterthanone(n))
        return n * fact(n-1);
    else
        return 1;
}

int main(){
    int ans;
    if(true)
        ans = fact(5);
    else
        ans = 0;
}